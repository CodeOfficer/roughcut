# Claude Code Project Context

## Project Overview
GenAI Tutorial Factory - Major overhaul to reveal.js-based presentation system.

**Current Work**: Building reveal.js + Playwright + ElevenLabs integration
**Branch**: `feat/revealjs-presentation`

---

## IMPORTANT: Read These Files First

### When Starting ANY Session
1. **`/planning/revealjs-integration/DECISIONS.md`** ← **START HERE ALWAYS**
   - All strategic decisions and user choices
   - Audio sync strategy (audio drives slides)
   - Chosen markdown format (Option 3 - Minimalist)
   - Pause-after-audio feature requirements
   - Terminology standards (use reveal.js primitives)
   - Output requirements (both HTML + MP4)
   - Testing approach, success criteria

2. **`/planning/revealjs-integration/IMPLEMENTATION_PLAN.md`**
   - 20-step detailed implementation plan
   - Organized into 9 phases
   - Step-by-step guide with test checkpoints

3. **`/planning/revealjs-integration/format-option-3-minimalist.md`** ← **CHOSEN FORMAT**
   - Markdown syntax specification
   - `@directive:` syntax for metadata
   - Complete examples and parsing strategy

4. **`/planning/revealjs-integration/revealjs-research.md`**
   - Complete reveal.js API documentation
   - Navigation methods, event system, Playwright integration

---

## Critical Decisions (From DECISIONS.md)

- **Format**: Option 3 (Minimalist with `@directive:` syntax)
- **Audio Sync**: Audio drives slides (with pause-after-audio feature)
- **Outputs**: Both interactive HTML + recorded MP4 video
- **Terminology**: ALWAYS use reveal.js primitives (slides, fragments, transitions, themes)
- **Testing**: Manual first, then automated before proceeding

---

## Current Status

- **Phase**: ✅ **BUILD SYSTEM FIXED** - All TypeScript errors resolved!
- **Build**: `npm run build` now passes with zero errors
- **Branch**: `feat/revealjs-presentation`
- **Tests**: 224 passing, 8 skipped
- **Commits**: 22 commits on feature branch
- **Next**: Ready for Step 14 or commit build fixes

**🔧 BUILD FIXES COMPLETE (2025-11-07)**:
- Fixed 100+ TypeScript compilation errors
- All strict type checking still enabled
- 12 files modified with proper null checks and type handling
- See `.context/build-fixes-completed.md` for full details
- See `.context/SESSION-SUMMARY.md` for quick reference

**✅ Step 13 COMPLETE**: CLI build command orchestrates complete pipeline! Parse → Audio → HTML → Timeline → Video → Assembly all working!

### 🎯 MAJOR MILESTONE: Core System Complete!

**The entire reveal.js video generation pipeline is FULLY FUNCTIONAL:**
- ✅ Markdown parsing with front matter format
- ✅ Audio generation with ElevenLabs API
- ✅ HTML generation with bundled reveal.js assets
- ✅ Timeline building with cumulative timing
- ✅ Video recording with Playwright browser automation
- ✅ FFmpeg video assembly with audio replacement
- ✅ Complete CLI build command orchestrating everything

**What works end-to-end:**
```bash
# Input: markdown file with front matter
# Output:
#   - presentation/index.html (interactive reveal.js)
#   - audio/*.mp3 (ElevenLabs narration)
#   - video/*.webm (recorded presentation)
#   - tutorial.mp4 (final video with audio)
```

**Remaining work is polish:**
- Steps 14-15: Additional CLI commands (nice-to-have)
- Steps 16-17: E2E integration tests (validation)
- Steps 18-20: Documentation and final polish

---

## New Architecture (Reveal.js Integration)

```
src/
├── core/
│   ├── revealjs-types.ts      # TypeScript interfaces
│   └── revealjs-parser.ts     # Markdown parser (Option 3)
│
├── presentation/               # NEW DIRECTORY
│   ├── revealjs-generator.ts  # HTML generation
│   ├── revealjs-assets.ts     # Asset bundling
│   ├── playwright-controller.ts  # Reveal.js control
│   ├── playwright-executor.ts    # Instruction execution
│   ├── audio-sync-orchestrator.ts # Audio-slide sync
│   └── audio-player.ts        # Browser audio playback
│
├── narration/
│   └── revealjs-speech.ts     # Audio per slide (ElevenLabs)
│
├── video/
│   ├── revealjs-timeline.ts   # Timeline builder
│   ├── playwright-recorder.ts # Video recording
│   └── revealjs-video-assembler.ts # Final assembly
│
└── cli/commands/
    └── revealjs-build.ts      # Build command
```

---

## Reveal.js Terminology (ALWAYS Use These)

**ALWAYS use reveal.js domain language:**
- **Slides** (not segments/sections/pages)
- **Horizontal slides** (main sequence, `---` delimiter)
- **Vertical slides** (nested optional content, `^^` delimiter)
- **Fragments** (step-by-step reveals within slide)
- **Transitions** (slide change animations)
- **Themes** (presentation styling)
- **Speaker notes** (presenter-only content)
- **Auto-animate** (smooth element transitions)

---

## Audio Sync Flow

```
1. Navigate to slide
2. Play audio for slide
3. Wait for audio to finish
4. Pause for @pause-after seconds (viewers can read content)
5. Advance to next slide
```

---

## Markdown Format (Option 3 - Minimalist)

```markdown
# Slide Title
@duration: 8s
@pause-after: 2s
@background: #1e1e1e
@transition: zoom

Content here...

- Point 1 @fragment
- Point 2 @fragment +2s

@audio: Narration here [2s] with pauses [3s] inline.

@playwright:
- Action: Click button
- Wait 2s
- Type: "text" + Enter
```

**See**: `/planning/revealjs-integration/format-option-3-minimalist.md` for full spec

---

## Dependencies to Install (Step 1)

```bash
npm install reveal.js
npm install -D @types/reveal.js
npm install -D @playwright/test
```

---

## Outputs

- Interactive HTML: `/tutorials/{name}/presentation/index.html`
- Video file: `/tutorials/{name}/output/tutorial.mp4`

---

## Key Files Reference

**Core Pipeline (all complete and working):**
- `src/core/revealjs-parser.ts` - Markdown parser with front matter (40 tests)
- `src/narration/revealjs-speech.ts` - ElevenLabs audio generation (11 tests)
- `src/presentation/revealjs-generator.ts` - HTML generation (18 tests)
- `src/presentation/revealjs-assets.ts` - Asset bundling (17 tests)
- `src/video/revealjs-timeline.ts` - Timeline builder (13 tests)
- `src/presentation/playwright-controller.ts` - Browser automation (21 tests)
- `src/presentation/playwright-executor.ts` - @playwright instructions (25 tests)
- `src/presentation/audio-sync-orchestrator.ts` - Master coordinator (7 tests)
- `src/video/playwright-recorder.ts` - Video recording (22 tests)
- `src/video/revealjs-video-assembler.ts` - FFmpeg assembly (17 tests)
- `src/cli/commands/revealjs-build.ts` - **CLI build command** (16 tests)

**All tests passing: 224 passing, 8 skipped**

---

## Context Management

**When this conversation context clears:**
1. Read `.claude.md` (this file) - **START HERE**
2. Review "Current Status" - **BUILD SYSTEM FIXED** ✅
3. Check `.context/SESSION-SUMMARY.md` for latest session work
4. See `.context/build-fixes-completed.md` for all TypeScript fixes
5. **Core system is COMPLETE** - all 5 phases done, CLI command working
6. Remaining steps 14-20 are polish (additional commands, tests, docs)
7. Read `/planning/revealjs-integration/DECISIONS.md` if needed for context
8. Reference `format-option-3-minimalist.md` for markdown syntax

---

## External Documentation

- Reveal.js: https://revealjs.com/
- Reveal.js markdown: https://revealjs.com/markdown/
- Playwright: https://playwright.dev/
